import Axios from 'axios';
import React from 'react';
import axios from 'axios';

const addReviewHandler = () => {

    function submit() {
        let combined = {
            ...article,
            reviews: reviews
        }
    }

    return (
        <div>

        </div>
    );
};

export default addReviewHandler;